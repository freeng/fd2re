# Copy this file to .env when the Emscripten SDK is not installed at the
# repository default path. The build script reads this file only; it does not
# depend on process environment variables.
EMCC=D:/workspace/python/emsdk/upstream/emscripten/emcc.bat
